import React from "react";
import { createRoot } from "react-dom/client";
import HelenasLernWeide from "../helenas-lern-weide.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <HelenasLernWeide />
  </React.StrictMode>
);
